-- Add script dependencies here if they were not added on their respective script.

-- This script is intended to be ran every time a change in the file happens. So there is no need to create another
-- script for every dependency found
